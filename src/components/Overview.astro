---
import type { Extension } from '@/types';
import { renderMarkdown } from '@/lib/markdown';

interface Props {
  ext: Extension;
}

const { ext } = Astro.props;
const readmeHtml = renderMarkdown(ext.readme ?? '');
---

<div class="markdown-body" set:html={readmeHtml} />
